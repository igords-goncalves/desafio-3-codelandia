import React from "react";
import Form from "./form/Form";
import "./Footer.css";

const Footer = (props) => {
    return (
        <footer>
            <div className="form">
                <Form />
            </div>
        </footer>
    );
};

export default Footer;
